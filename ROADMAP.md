# خارطة طريق سندك الطبي — منهجية Agile

مصدرها ثلاثة تدقيقات حيّة متتالية على النسخة الحيّة (مساعد الشات، UX
بستة سيناريوهات، وتدقيق شامل متعدد التخصصات بدرجة إجمالية 51/100). كل
Epic هنا قصة مستخدم حقيقية بمعيار قبول واضح، لا مهمة تقنية مجردة — ومصدرها
مذكور صراحة (رقم القسم في تقرير 22-إشكالية أو التدقيق الشامل).

**قاعدة ثابتة:** أي عنصر يُنجَز يُنقَل هنا فوراً بتاريخه ودليل تحقّقه (سطر
كود، رابط مُختبَر) — لا "تم" بلا دليل، تماماً كما التزم تقرير التدقيق نفسه
بعبارة "لم أتمكن من التحقق" حين يلزم.

---

## Epic 0 — إصلاحات فورية (منجَزة عبر Sprint 1-6، جلسة واحدة)

هذه كانت أعلى 10 مشاكل أثراً في تقرير "٢٢ إشكالية" وخطة الأيام السبعة —
نُفِّذت بالكامل ونُشرت على `main` فعلياً:

| # التقرير | المشكلة | الحل المنفَّذ | الملف |
|---|---|---|---|
| #1 | البحث الرئيسي لا يفهم التخصص | مطابقة كلمة-بكلمة ضد أسماء التخصصات الحقيقية | `js/doctors.js` |
| #2 | المدينة غائبة عن بطاقة/صفحة الطبيب | `get-doctors` يُضمِّن `facility_doctors→facilities`؛ `doctorLocationLabel()` | `supabase/functions/get-doctors`, `js/common.js` |
| #5 | حساسية الإملاء (ة/ه) تُعيد صفر نتائج | `spellingVariants()` — إعادة محاولة تلقائية | `js/common.js` (مشترك) |
| #6 | "طيب" العامية تُفسَد بها المدينة (مساعد الشات) | إضافتها لقائمة كلمات الطبيب | `js/assistant.js` |
| #7 | لا استجابة أمان لأعراض طارئة (مساعد الشات) | نيّة `emergency` بأعلى أولوية مطلقة | `js/assistant.js` |
| #11 | الوصول للخريطة يحتاج ٤ نقرات | زرّ خريطة بنقرة واحدة على صفحة الطبيب | `js/doctor.js` |
| #4 (جزئي) | صفر Meta Description/H1 | وُسم `<meta description>` ثابت لكل صفحة + ديناميكي لصفحات السجلّ، وتحويل عناوين `title-lg` إلى `<h1>` حقيقية | كل صفحات HTML + `js/common.js` |
| #8 (جزئي) | لا robots.txt/sitemap.xml | ملفان ثابتان يغطّيان الصفحات الأساسية (لا صفحات السجلّ الفردية بعد — انظر Epic 3) | `robots.txt`, `sitemap.xml` |

**اختبارات آلية:** `js/assistant.test.js` — ٢٩ اختباراً، تُشغَّل بـ`node js/assistant.test.js`.

---

## Epic 1 — البحث والاكتشاف (Search & Discovery: 38→الهدف 75)
*المصدر: تدقيق شامل §7، §22 (#1، #17)*

- [x] البحث الرئيسي يفهم التخصص (Sprint 6)
- [x] تطبيع الإملاء (ة/ه) في بحث الأطباء والمرافق (Sprint 6)
- [ ] **فلتر مدينة في صفحة قائمة الأطباء** (يوازي فلتر التخصص الموجود) — لا حاجة API جديد، `get-doctors` لا يدعم فلترة بمدينة المرفق المضمَّن مباشرة؛ الأسهل: فلترة الفلترة محلياً على `doctors.js` بعد الجلب باستخدام `doctorPrimaryFacility(d).city` الذي بنيناه أصلاً في Epic 0.
- [ ] ترتيب افتراضي بالمدينة المختارة أولاً (نفس مبدأ `schedule_query` في المساعد، مُطبَّق هنا على `doctors.js`/`facilities.js`).
- [ ] رسائل "لا نتائج" ذكية تقترح بدائل (تخصص قريب، مدينة مجاورة) بدل جملة عامة واحدة مكرَّرة في كل سياق.
- [ ] Autocomplete أثناء الكتابة (اقتراح أسماء/تخصصات مطابقة قبل الضغط "بحث").
- [ ] **تحويل جذري (خارج نطاق سبرنت واحد):** حقل بحث موحَّد واحد يفهم (اسم + تخصص + مدينة) بمنطق خلفي موحَّد — يحتاج تصميم واجهة جديد، هدف Epic منفصل لاحقاً (Epic 7).

## Epic 2 — GPS و"الأقرب مني" (Yemen Market Fit + UX)
*المصدر: UX §Scenario B، §22 (#3)*

- [ ] زر "استخدم موقعي الحالي" (Geolocation API) في صفحة المرافق.
- [ ] فرز بالمسافة الفعلية (haversine على `latitude`/`longitude` الموجودين أصلاً في جدول `facilities` — تحقّقتُ: العمودان موجودان في `get-facilities` select الحالي لكن غير مُستخدَمين في أي فرز).
- [ ] بديل يدوي: قائمة مدن ثابتة بدل حقل نصّ حرّ (يمنع أخطاء الإملاء من الأساس، لا يعالجها بعد وقوعها).
- [ ] تمييز بصري للمرافق التي لديها قسم طوارئ فعلي (يحتاج عمود `has_emergency` جديد — **تغيير مخطط قاعدة بيانات**، Epic منفصل يحتاج قرار منتج أولاً: من يُدخل هذه البيانات ومتى تُراجَع).

## Epic 3 — SEO الكامل (15→الهدف 60)
*المصدر: تدقيق شامل §10، §22 (#4، #8)*

- [x] Meta description + H1 لكل صفحة قائمة وسجلّ (Sprint 6)
- [x] `robots.txt` + `sitemap.xml` للصفحات الثابتة (Sprint 6)
- [ ] **Sitemap ديناميكي لكل طبيب/مرفق (415+/106+ سجلّ)** — يحتاج دالة حافة جديدة (`sitemap-dynamic` مثلاً) تستعلم القاعدة وتُعيد XML حقيقياً، أو ملف يُولَّد بسكربت بناء (لا يوجد خط بناء حالياً في هذا المشروع الثابت — قرار معماري: إمّا دالة حافة تُستدعى من `sitemap.xml` (لا يعمل مع GitHub Pages الساكن بدون توجيه) أو نطاق فرعي/دالة منفصلة تُسجَّل في Search Console مباشرة كمصدر sitemap إضافي.
- [ ] Schema.org JSON-LD (`Physician`, `MedicalClinic`, `LocalBusiness`) لكل صفحة سجلّ — يفتح Rich Results في جوجل.
- [ ] صفحات هبوط ثابتة القالب لكل (مدينة × تخصص) — أكبر فرصة نمو عضوي مجاني؛ تحتاج قالب صفحة جديد + توليد الروابط من بيانات `specialties`×`facilities.city` الموجودة فعلاً.
- [ ] Breadcrumb مرئي + `BreadcrumbList` schema.
- [ ] OpenGraph/Twitter Cards (مشاركة روابط أنيقة على واتساب — قناة التواصل الأساسية في اليمن، أثر مضاعف هنا تحديداً).

## Epic 4 — الثقة وجودة البيانات (Trust: 50→الهدف 75، Data Quality: 55→75)
*المصدر: تدقيق شامل §8، §14، §22 (#9، #10، #18)*

- [ ] حقل "آخر تحديث" — يحتاج التحقق أولاً: هل `updated_at` موجود فعلاً على `doctors`/`facilities`؟ (**لم أتحقق بعد** — أول خطوة تنفيذ فعلية لا افتراض).
- [ ] زر "أبلغ عن معلومة خاطئة" — نموذج بسيط (اسم الحقل + وصف) يُخزَّن في جدول جديد `data_reports` أو يُرسَل بدالة حافة بسيطة لبريد الفريق.
- [ ] إخلاء مسؤولية طبي صريح وثابت (نص قانوني، لا يحتاج كوداً معقداً — أرخص عنصر في كامل الخارطة).
- [ ] رابط صفحة "شروط الاستخدام" منفصلة عن سياسة الخصوصية (**لم أتحقق: هل موجودة وغير مرتبطة، أم غير موجودة إطلاقاً؟**).
- [ ] نظام Data Quality Score لكل سجلّ (المقترح في التدقيق §8) — منطق حساب بسيط، لكن يحتاج قراراً منتجياً: هل يُعرض للمستخدم أم داخلي فقط للفريق أولاً؟
- [ ] تنظيف الأسماء المشوَّهة ("د. د: انور سنجل") — عمل بيانات يدوي/سكربت تنظيف لمرة واحدة، ليس ميزة كود دائمة.
- [ ] شارة "مفتوح الآن/اليوم" محسوبة من `working_days`/`period`/`start_time`/`end_time` الموجودين أصلاً — منطق عرض بحت، لا بيانات جديدة.

## Epic 5 — الأداء (62→الهدف 80)
*المصدر: تدقيق شامل §11*

- [ ] تجميع استدعاءات الصفحة الرئيسية (5 نداءات منفصلة اليوم) في نداء واحد أو نداءين متوازيين بحد أقصى.
- [ ] تخزين مؤقت من جهة العميل (`sessionStorage` كحدّ أدنى بسيط، أو مكتبة SWR إن أُضيف خط بناء لاحقاً).
- [ ] Pagination حقيقي لقوائم الأطباء بدل جلب الكل (حالياً `limit: 60` صريح في `doctors.js`/`facilities.js` — يحتاج زرّ "تحميل المزيد" أو تمرير لا نهائي).
- [ ] ضغط صور الشعارات قبل الرفع (جهة الإدارة — خارج نطاق الواجهة العامة التي دُقِّقت).

## Epic 6 — الأمان (55→الهدف 75)
*المصدر: تدقيق شامل §12*

- [ ] رؤوس HTTP قياسية (HSTS، X-Frame-Options، Referrer-Policy) — **قيد معماري حقيقي**: GitHub Pages لا يسمح بتخصيص رؤوس HTTP إطلاقاً. الحل الوحيد: طبقة Cloudflare (أو مشابه) أمام النطاق، وهذا قرار بنية تحتية لا كود، يحتاج نقاش مالك النطاق `snadk.codeysaa.com`.
- [ ] تحويل CSP من `<meta>` إلى رأس HTTP حقيقي — نفس القيد أعلاه بالضبط.
- [ ] مؤشر قوة كلمة المرور في نموذج التسجيل.

## Epic 7 — إعادة تصميم تجربة المريض (بناءً على المخطط المقترح بالتدقيق)
*المصدر: تدقيق شامل §"أفضل تصميم مقترح للصفحة الرئيسية"، UX §User Flow*

هذا Epic تصميمي كبير يحتاج قراراً من صاحب المنتج قبل أي كود — التوصية:
اختيار مدينة إجباري بصرياً كخطوة أولى (قائمة ثابتة أو GPS)، حقل بحث واحد
يفهم الاسم والتخصص معاً، زر طوارئ ثابت الظهور. **لا يُنفَّذ تلقائياً هنا**
لأنه يعيد تشكيل الصفحة الرئيسية بالكامل — يحتاج موافقة صريحة قبل البدء.

## Epic 8 — نموذج العمل (Business Model: 60)
*المصدر: تدقيق شامل §15، §22 (#20)*

- [ ] **توثيق فوري (لا كود):** التحقق من آلية استقبال مدفوعات Stripe فعلياً
  في اليمن (التقرير يشير لعدم دعم Stripe الرسمي لليمن) — سؤال لفريق العمل
  المالي/القانوني، ليس مهمة تقنية.
- [ ] إبقاء الحجز الإلكتروني/الدفع اختيارياً متقدماً لا شرطاً لتفعيل مرفق.
- [ ] شارة توثيق مجانية للمرافق مكتملة البيانات (تحفّز الجودة بلا مقابل مادي).

## Epic 9 — الاحتفاظ (Retention: 35→الهدف 55)
*المصدر: تدقيق شامل §20*

- [ ] تذكيرات مواعيد فعلية عبر FCM (البنية موجودة حسب سياسة الخصوصية — **لم أتحقق من التفعيل الفعلي**).
- [ ] "أطبائي المحفوظون" (قائمة مفضّلة بسيطة، تحتاج حساب مستخدم مسجَّل فعّال).
- [ ] إشعار مخيمات طبية جديدة قريبة من مدينة المستخدم.

---

## كيف يُقرأ هذا الملف

- `[x]` = منجَز ومنشور فعلياً على `main`، بدليل (سطر كود أو رابط مُختبَر أعلاه في Epic 0).
- `[ ]` بلا ملاحظة = جاهز للتنفيذ في سبرنت قادم بلا عائق معماري.
- `[ ]` بملاحظة **قيد معماري/قرار منتج مطلوب** = لا يُبدَأ به قبل حسم ذلك القرار صراحةً — تنفيذه بلا قرار مسبق مخاطرة لا تناسب منتجاً صحياً حيّاً.
- أي بند "لم أتحقق منه" هنا يعني: الخطوة الأولى فعلياً هي التحقق نفسه، لا افتراض الجواب.
